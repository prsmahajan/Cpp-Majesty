#include <iostream>
using namespace std;
int main()
{
    int sum = 0, a[7] = {1, 2, 3, 4, 5, 6, 7};

    for (int i = 0; i < a[i]; i++)
    {
        sum += a[i];
    }
    cout << sum;
    return 0;
}